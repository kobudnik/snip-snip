version: '3'
services:
  snip_app:
    image: kbud93/snip_elephant
    container_name: snip_elephant
    environment:
      PG_URI: ${PG_URI}
    ports:
      - '3010:3000'
    depends_on:
      - redis

  redis:
    image: redis
    container_name: redis
